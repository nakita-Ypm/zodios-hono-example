{
  "name": "@packages/domain",
  "private": true,
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@packages/prisma": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.4",
    "vitest": "^2.1.4"
  }
}
